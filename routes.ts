
/**
 * Represents the public routes.
 * @type {string[]}
 */
export const publicRoutes = [
    "/"
]

/**
 * Represents the authentication routes.
 * @type {string[]}
 */
export const authRoutes = [
    "/auth/login",
    "/auth/signup",
]


/**
 * Represents the api prefix.
 * @type {string[]}
 */
export const apiRoutesPrefix = "/api"

/**
 * entry point for if user is logedIn
 * @type {string}
 */
export const DEFAULT_LOGEDIN_URL = "/settings"